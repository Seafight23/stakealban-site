export const metadata = { title: "Stake Alban", description: "Leaderboard" };

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
